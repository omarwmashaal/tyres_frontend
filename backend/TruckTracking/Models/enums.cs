namespace TruckTracking.Models
{
    public enum Enum_TyreSide { Right, Left }
    public enum Enum_TyreDirection
    {
        Single,
        Inner,
        Outer,
    }
    public enum Enum_TyreLog
    {
        Added,
        Installed,
        Uninstalled,
        Removed,
    }
}
